const RESOURCE_PATH = '/../../resources';

var fs = require('fs'),
    path = require('path');

exports.load = function(filename) {
    var file = path.resolve(__dirname + RESOURCE_PATH, filename);

    return fs.readFileSync(file, 'UTF-8');
}