const ROOT_PATH = '/../../';

var fs = require('fs'),
    path = require('path'),
    cache = {};

exports.load = function(filename) {
    if (!cache[filename]) {
        var file = path.resolve(__dirname + ROOT_PATH, filename);

        cache[filename] = fs.readFileSync(file, 'UTF-8');
    }

    return cache[filename];
}